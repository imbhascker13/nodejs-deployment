- name: Install npm 
  apt: name=npm state=present
- name: Copy Feature_3 Code 
  copy: src=../code/. dest=/var/www/{{ http_host3 }}

- name: Start Application
  command: chdir=/var/www/{{ http_host3 }}  npm install --production
  notify: 
   - Restart Apache
